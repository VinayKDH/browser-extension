{
  "name": "tens-ai-browser-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Open and search Tens AI quickly from your browser",
  "license": "MIT",
  "scripts": {
    "lint": "web-ext lint",
    "start": "web-ext run --verbose --no-reload --start-url https://dev2.tens-ai.com/ | cat",
    "build": "web-ext build -o",
    "test": "node test/run-tests.js",
    "test:browser": "echo 'Open test/test.html in your browser after loading the extension'"
  },
  "devDependencies": {
    "web-ext": "^7.11.0"
  }
}

